Q1. Calculator using logging
Q2. Automated email
Q3. Backup source -> desti folder

Q4. Create a multithreaded Python program where one thread computes the sum of      squares from 1–50, another identifies all prime numbers between 1–20, and a third thread counts the results generated by the other two threads.

Q5. Build a Python simulation for an aircraft that continuously displays its altitude and speed, logs all flight events to a file, and sends an alert email in case of a critical situation.

Q6. Design a Python script using pyautogui that automates clicking actions around a circular path on the screen.

Q7. Using Pandas, perform analysis to:
- Detect undelivered orders
- Find the most frequently returned products
- Identify the top two customers by total revenue
- Merge multiple datasets into one
- Save and reload data in CSV format

Q8. Perform EDA on heart disease dataset